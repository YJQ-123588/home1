# 西游记 - 祸起观音院

一款基于Pygame开发的2D RPG冒险游戏，以中国古典名著《西游记》中"祸起观音院"为故事背景。

## 游戏特点

- 经典的2D RPG游戏玩法
- 回合制战斗系统
- 多场景探索（村庄、寺庙）
- 丰富的NPC对话和剧情
- 精美的像素风格角色动画
- 背景音乐和音效支持

## 游戏操作

### 移动控制
- **方向键/WASD**：控制孙悟空移动
- **Shift**：加速移动
- **空格/回车**：关闭对话框、确认选择
- **ESC**：退出游戏

### 战斗操作
- **数字键1**：普通攻击
- **数字键2**：技能攻击（如意金箍棒）
- **数字键3**：大招（七十二变，需解锁）
- **鼠标点击按钮**：也可选择攻击方式

### 存档操作
- **T键**：保存当前游戏进度（最多3个存档位）
- **读档界面**：按数字键1/2/3快速读取对应存档

## 游戏流程

1. **开始界面**：点击"开始游戏"或按回车键开始
2. **村庄场景**：与NPC对话，了解剧情，准备前往寺庙
3. **寺庙场景**：探索寺庙，击败小怪（M1-M3）
4. **BOSS出现**：击败所有小怪后，牛魔王（M4）才会出现
5. **战斗系统**：回合制战斗，选择攻击方式击败敌人
6. **游戏通关**：成功降服观音院妖怪

## 项目结构

```
PythonProject1-pygame1/
├── main.py                    # 游戏主入口
├── README.md                  # 项目说明文档
├── core/                      # 核心模块
│   ├── __init__.py
│   ├── game.py                # 游戏主控类
│   ├── animation.py           # 动画系统
│   └── tiled_map.py           # Tiled地图加载
├── scenes/                    # 场景模块
│   ├── __init__.py
│   ├── start_scene.py         # 开始界面
│   ├── village_scene.py       # 村庄场景
│   ├── temple_scene.py        # 寺庙场景
│   └── fight_scene.py         # 战斗场景
├── characters/                # 角色模块
│   ├── __init__.py
│   ├── player.py              # 玩家角色（孙悟空）
│   └── npc.py                 # NPC角色
├── ui/                        # UI模块
│   ├── __init__.py
│   ├── dialog.py              # 对话框
│   ├── confirm_dialog.py      # 确认对话框
│   ├── fade_scene.py          # 渐入渐出效果
│   └── avoid_battle_dialog.py # 避战对话框
├── config/                    # 配置模块
│   ├── __init__.py
│   └── settings.py            # 游戏设置和常量
├── resources/                 # 游戏资源文件
│   ├── img/                   # 图片资源
│   │   ├── swk/              # 孙悟空图片
│   │   ├── swk2/             # 孙悟空战斗图片
│   │   ├── cattle/           # 牛妖图片
│   │   ├── god/              # 土地公图片
│   │   ├── elder/            # 长老图片
│   │   ├── magic/            # 魔法特效
│   │   ├── button/           # 按钮图片
│   │   ├── dialog/           # 对话框图片
│   │   ├── village.jpg       # 村庄背景
│   │   ├── temple.jpg        # 寺庙背景
│   │   ├── win.jpg           # 胜利画面
│   │   └── fail.jpg          # 失败画面
│   ├── sound/                 # 音频资源
│   │   ├── nmw.mp3           # 村庄背景音乐
│   │   ├── aigei.mp3         # 寺庙背景音乐
│   │   └── swk.wav           # 战斗音效
│   ├── tmx/                   # 地图文件
│   │   ├── village1.tmx      # 村庄地图
│   │   └── temple1.tmx       # 寺庙地图
│   └── font/                  # 字体文件
│       └── newfont.TTF        # 游戏字体
└── docs/                      # 文档
    ├── 概要设计说明书.tex     # LaTeX源文件
    └── 概要设计说明书.pdf     # 设计文档
```

## 运行要求

### Python版本
- Python 3.6+

### 依赖库
- Pygame 2.0+
- pytmx（用于加载Tiled地图）

### 安装依赖
```bash
pip install pygame pytmx
```

## 安装与运行

1. 克隆或下载本项目
2. 确保已安装Python 3.6+
3. 安装依赖库：
   ```bash
   pip install pygame pytmx
   ```
4. 运行游戏：
   ```bash
   python main.py
   ```

**注意**：游戏资源文件已包含在 `resources/` 目录中，无需额外下载。

## 游戏配置

游戏配置文件位于 `config/settings.py`，可调整以下参数：

- **窗口设置**：屏幕尺寸（800x600）、帧率（30FPS）
- **玩家属性**：移动速度、基础生命值（100）、基础攻击力（25）
- **战斗设置**：普通攻击伤害（25）、技能伤害（40）、大招伤害（70）
- **怪物属性**：普通怪物生命值（100）、BOSS生命值（250）

## 开发说明

### 代码风格
- 使用中文注释
- 遵循Python PEP 8编码规范
- 采用面向对象设计模式

### 扩展性
- 场景系统易于扩展新场景
- 角色系统支持添加新NPC
- 战斗系统可扩展新技能和攻击方式

## 已知问题

- 部分资源文件缺失时游戏会使用默认颜色填充

## 更新日志

### 2026-06-26
- 新增 WASD 按键支持移动
- 寺庙场景机制调整：需先击败所有小怪（M1-M3），牛魔王（M4）才会出现
- 更新寺庙地图文件
- 游戏介绍界面支持鼠标滚轮滚动
- 修复同时使用方向键和WASD导致角色卡住的问题
- 长老3（治疗）：仅在玩家血条不满时回血
- 长老1和长老3：未解锁大招时会提示去找长老2学艺
- 新增存档功能（按T存档，最多3个存档位，支持读档和删档）

## 许可证

本项目仅供学习和研究使用。